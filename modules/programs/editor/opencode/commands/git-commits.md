# Commit Command

Create a git commit with proper message formatting.
Usage: /commit [message]


1. always use : "modules: "[changes made to that module]
2. this shouldn't mean that you have to commit file per file.
